# 360dialog / WhatsApp
WHATSAPP_API_KEY=
WEBHOOK_VERIFY_TOKEN=sinan2024
# Opcional — token directo de Meta, solo se usa como fallback cuando 360dialog
# devuelve una URL de media en el CDN de Meta (lookaside.fbsbx.com) en vez de
# una propia. El bot funciona sin esto, pero la descarga de imágenes desde esas
# URLs fallará con 401 si no está seteado.
META_TOKEN=

# Postgres — Railway lo inyecta automáticamente al añadir el addon, no lo seteen manualmente en Railway
DATABASE_URL=postgres://user:pass@host:port/db

# Claude / Anthropic
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-sonnet-4-5

# Google Drive — Service Account (JSON completo del service account, codificado en base64)
GOOGLE_SERVICE_ACCOUNT_JSON_BASE64=
GOOGLE_DRIVE_FOLDER_ID_BOLETAS=
GOOGLE_DRIVE_FOLDER_ID_BACKUPS=
GOOGLE_DRIVE_FOLDER_ID_REPORTES=

# Backups y resumen diario (formato cron estándar)
BACKUP_CRON_SCHEDULE=0 6 * * *
BACKUP_TIMEZONE=America/Santiago
RESUMEN_DIARIO_CRON_SCHEDULE=0 8 * * *

PORT=3000
